

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_BASE_URL: ${API_BASE_URL}
    container_name: ptmaja-web
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-80}:80"
